# ERS work files — session 2026-07-14

Two gate cycles run this session, both COMMITTED. Drop into
`welwordion-sys/ERS/work_files/`.

## worldmodel_transplant_gate.json  (committed: ans)
Decision: transplant the reasoning_engine world-model into ERS as a CONSISTENCY
layer — checks referent-existence and contradiction-against-populated-relations.
Explicitly NOT a truth oracle.
Falsifier bit: broad "verifies truth" reading FALSIFIED (consistent-with-a-wrong-
model is still wrong). Only the narrow consistency claim survives.
Reproducer: gate_worldmodel.py

## world_model_format_gate.json  (committed: ans_b)
Decision: propagation licensed per (predicate, relation, direction) CHANNEL by
explicit declaration; undeclared channels inert; a relation's own transitivity is
itself just a declared channel. No blanket transitivity, no exception mechanism.
Falsifier bit: strong claim "cannot contaminate" FALSIFIED — a wrongly-declared
channel propagates falsehood soundly-by-construction. Survives only as: cannot
contaminate WITHOUT an explicit, attributable declaration.
Reproducer: gate_wm_format.py then gate_fix.py

### KNOWN GAP — half-gated
`ans_a` (the v0 at-most-one-of collision primitive: subsumes functional slots,
dimension-value exclusion, category disjointness, antisymmetry, irreflexivity;
depth-0 lookup only) is GROUNDED AND CHECKED BUT **NOT COMMITTED**. ERS freezes
state on commit (I6), so it could not be committed in the same cycle. It needs
its own cycle. Do not treat the v0 primitive as gated.

Also filed against ans_a (from chk_falsify_a):
- at-most-one-of CANNOT express "exactly one"/totality — v0 detects collisions,
  does not enforce completeness. [filed]
- cross-dimension incompatible pairs (transparent+dark) must be hand-enumerated
  per domain; does not generalize. [carried]

## claims_registry.json
Now 14 entries — v0.6 commit() auto-appended both closures. This copy supersedes
the one in the reasoning_engine zip if that one is older.
